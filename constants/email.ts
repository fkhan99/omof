/** Shown in-app so users know which inbox to check. */
export const VERIFICATION_SENDER_EMAIL = 'omofverification@gmail.com';

/** Firebase Auth default when Cloud Function SMTP is unavailable. */
export const FIREBASE_AUTH_FALLBACK_SENDER = 'noreply@omof-eed24.firebaseapp.com';
