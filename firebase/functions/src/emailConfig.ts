/** Gmail account used to send OMOF verification emails via Cloud Functions SMTP. */
export const VERIFICATION_SMTP_USER = 'omofverified@gmail.com';

export const VERIFICATION_SMTP_FROM = `OMOF <${VERIFICATION_SMTP_USER}>`;
